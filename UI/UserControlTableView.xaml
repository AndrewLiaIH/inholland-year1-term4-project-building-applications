<UserControl x:Class="UI.UserControlTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="clr-namespace:UI"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ui:UserControlHeader x:Name="userControlHeader" Grid.Row="0"/>
        <ContentControl x:Name="TableViewContentControl" Grid.Row="1"/>
        <ContentControl x:Name="NetworkErrorView" Grid.Row="1"/>
    </Grid>
</UserControl>