<UserControl Loaded="TableLoaded_Load" x:Class="UI.UserControlTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="clr-namespace:UI"
             mc:Ignorable="d">
    
    <Grid>
        <Grid Margin="50">
            <Border x:Name="RoundedBorder" Style="{StaticResource TableRoundedBorderStyle}">
                <Grid Style="{StaticResource TableGridStyle}" Width="190" HorizontalAlignment="Right">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" x:Name="ButtonFree" Content="Free" Style="{StaticResource FreeButton}" Click="ButtonFree_Click" Margin="10,6,25,10"/>
                        <Image x:Name="BellIcon" Grid.Row="0" Grid.Column="1" Source="{StaticResource BellIcon}" Style="{StaticResource TableBellIcon}" Width="20" Height="20" Margin="66,14,9,14" RenderTransformOrigin="-0.643,0.553"/>
                    </Grid>
                    <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Grid.Column="0" Text="Table 1" Style="{StaticResource TableNameStyle}"/>
                    </Grid>
                    <Grid Grid.Row="2" HorizontalAlignment="Center">
                        <TextBlock Grid.Column="0" Padding="0,3,0,0" Text="5 Minutes"/>
                    </Grid>
                    <Grid Grid.Row="3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button x:Name="ButtonReserve" Grid.Column="0" Content="Reserve" Style="{StaticResource ButtonReserveStyle}" Click="ButtonReserve_Click"/>
                        <Button x:Name="ButtonOrder" Grid.Column="1" Content="Order" Style="{StaticResource ButtonOrderStyle}" Click="ButtonOrder_Click"/>
                        <Button x:Name="ButtonServed" Grid.Column="1" Content="Served" Style="{StaticResource ButtonServedStyle}" Click="ButtonServed_Click"/>
                        <Button x:Name="ButtonEditOrder" Grid.Column="0" Content="Edit Order" Style="{StaticResource ButtonEditOrderStyle}" Click="ButtonEditOrder_Click"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
