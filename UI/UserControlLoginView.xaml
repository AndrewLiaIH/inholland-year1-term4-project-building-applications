<UserControl x:Class="UI.UserControlLoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Width="1600" Height="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="250"/>
            <RowDefinition Height="550"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Row="1" Grid.Column="0" Background="{StaticResource Color1}">
            <StackPanel Width="400" Height="300" Background="{StaticResource White}">

                <TextBlock Text="User ID:" Foreground="{StaticResource TextDark}"/>
                <TextBox x:Name="UserLoginTextBox"/>

                <TextBlock Text="Password:" Foreground="{StaticResource TextDark}"/>
                <TextBox x:Name="PasswordTextBox"/>

                <Button Style="{StaticResource FancyButtonStyle}" Content="Login" Click="ClickHandler"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="1" Grid.Column="1" Background="{StaticResource White}">
        </Border>
    </Grid>
</UserControl>